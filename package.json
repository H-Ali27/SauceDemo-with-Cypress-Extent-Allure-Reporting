{
  "dependencies": {
    "cypress": "^13.6.6",
    "init": "^0.1.2"
  },
  "scripts": {
    "browser:chrome": "npx cypress run --browser=chrome --headed --spec cypress/Execution/ChkoutSummary.cy.js --reporter mocha-allure-reporter",
    "report:allure": "allure generate allure-results --clean -o allure-report && allure open  allure-report"
  },
  "devDependencies": {
    "allure-commandline": "^2.27.0",
    "cypress-mochawesome-reporter": "^3.8.2",
    "mocha-allure-reporter": "^1.4.0"
  }
}
